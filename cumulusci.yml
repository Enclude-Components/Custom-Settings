minimum_cumulusci_version: '3.93.0'
project:
    name: Custom-Settings
    package:
        name: Custom-Settings
        api_version: '62.0'
    git:
        default_branch: 'main'
    source_format: sfdx

tasks:
    run_tests:
        options:
            required_org_code_coverage_percent: 75
